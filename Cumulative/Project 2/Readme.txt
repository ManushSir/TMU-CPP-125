Write a program to read in 3 integer values and then print them out in ascending order. 
• In each case, the main func on should read the values, call helper func on(s) to determine the highest and lowest values, calculate the middle value (a + b + c – min – max), and print the values in ascending order. 
• In the program, you should use two helper func ons, int minof3(int a, int b, int c);  /* This returns the smallest of the three numbers a, b, and c */ int maxof3(int a, int b, int c);   /* This returns the largest of the three numbers a, b, and c */ 

Create 2 different sets of data of your own to test the program. The test data should contain input values in different possible orders, with duplicate values, and with posi ve, zero, and nega ve values. 
